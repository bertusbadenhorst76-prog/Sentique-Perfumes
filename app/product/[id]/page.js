import {notFound} from 'next/navigation';import {products} from '@/data/products';import ProductDetails from './product-details';export function generateStaticParams(){return products.map(p=>({id:p.id}))}export function generateMetadata({params}){const p=products.find(x=>x.id===params.id);return {title:p?.name||'Perfume',description:p?.description}}export default function Product({params}){const p=products.find(x=>x.id===params.id);if(!p)notFound();const similar=products.filter(x=>x.id!==p.id&&(x.category===p.category||x.brand===p.brand)).slice(0,4);return <ProductDetails product={p} similar={similar}/>}
